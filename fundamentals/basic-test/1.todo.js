/*
 
 * Instructions: Write tests in plain JavaScript to be able
   to catch bugs in the working code.
 
 > RUN node 1.todo.js to check if your code correctly caught the bug
 
 */

// function sum is broken intentionally so it will fire an error.
const sum = (a, b) => a - b
const subtract = (a, b) => a - b